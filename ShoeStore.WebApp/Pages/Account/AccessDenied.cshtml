@* 
   Страница "Доступ запрещен"
   Отображается пользователям при попытке доступа к ресурсам без соответствующих прав
*@

@page
@{
    ViewData["Title"] = "Доступ запрещен";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="filters-panel">
            <h2 class="filter-title text-center"><i class="fas fa-ban"></i> Доступ запрещен</h2>

            <div class="text-center py-4">
                @* Иконка предупреждения *@
                <i class="fas fa-exclamation-triangle fa-4x text-warning mb-3"></i>
                <h4>У вас нет доступа к этой странице</h4>
                <p class="text-muted">Обратитесь к администратору для получения прав доступа.</p>

                @* Кнопка возврата на главную страницу *@
                <div class="mt-4">
                    <a asp-page="/Index" class="btn btn-accent">
                        <i class="fas fa-home"></i> На главную
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>